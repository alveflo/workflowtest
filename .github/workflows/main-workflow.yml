name: Main Workflow

on:
  push:
    branches:
      - main

jobs:
  trigger-reusable:
    uses: ./.github/workflows/reusable-workflow.yml
    with:
      dummy_parameter: 'test-value-from-main-workflow'
